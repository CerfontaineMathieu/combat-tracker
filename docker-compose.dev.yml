services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "3001:3000"
    environment:
      - DATABASE_URL=postgresql://dnd:dnd@db:5432/dnd_tracker
      - WATCHPACK_POLLING=true
      - NOTION_API_TOKEN=${NOTION_API_TOKEN}
      - NOTION_MONSTERS_DATABASE_ID=${NOTION_MONSTERS_DATABASE_ID}
    volumes:
      # Mount source code for hot-reload
      - .:/app
      # Exclude node_modules to use container's version
      - /app/node_modules
      - /app/.next
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  db:
    image: postgres:18
    environment:
      POSTGRES_USER: dnd
      POSTGRES_PASSWORD: dnd
      POSTGRES_DB: dnd_tracker
    ports:
      - "5433:5432"
    volumes:
      - postgres_data_dev:/var/lib/postgresql
      - ./docker/init-db:/docker-entrypoint-initdb.d
      - ./public/all_monsters.csv:/data/all_monsters.csv:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dnd -d dnd_tracker"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  postgres_data_dev:
